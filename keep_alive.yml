name: Keep Bot Alive

on:
  schedule:
    - cron: '*/5 * * * *'  # هر ۵ دقیقه یکبار اجرا میشه
  workflow_dispatch:  # این خط رو اضافه کن تا بتونی دستی هم اجراش کنی

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping the bot
        run: |
          curl -I https://mamaddd12412.onrender.com
